# Repository Metadata

This repository contains the open-source Python implementation of Feature
Prototype Direction Explainer (FPDE), a prototype-contrast feature attribution
method for classification models.

## Canonical Repository

```text
https://github.com/fpde-xai/fpde
```

## Package Names

| Registry or runtime | Name |
| --- | --- |
| PyPI distribution | `fpde` |
| Python import package | `fpde` |

## Suggested GitHub Description

```text
Open-source Python implementation of Feature Prototype Direction Explainer (FPDE).
```

## Suggested Topics

```text
machine-learning
explainable-ai
xai
feature-attribution
prototype-based
interpretable-machine-learning
python
```

## Important Paths

| Path | Purpose |
| --- | --- |
| `src/fpde/` | Installable Python package. |
| `tests/` | Pytest coverage for package metadata and core behavior. |
| `examples/` | Runnable example scripts and notebooks. |
| `docs/` | Method, API, reproducibility, and repository documentation. |
| `README.md` | Project overview and quick start. |
| `RELEASE_NOTES.md` | Version history. |
| `CITATION.cff` | Citation metadata for academic use. |
| `pyproject.toml` | Build metadata and project configuration. |
| `Dockerfile` | Containerized test-and-example workflow. |

## Supported Python Versions

The package metadata requires Python 3.12 or newer and includes classifiers for
Python 3.12, 3.13, and 3.14.

## Dependencies

Runtime and test dependencies are intentionally lightweight:

- NumPy
- scikit-learn
- pytest

## Licensing

The repository is distributed under the Apache License 2.0. The license text is
available in `LICENSE`.
